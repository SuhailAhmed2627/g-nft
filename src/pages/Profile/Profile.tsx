import React from "react";
import styles from "./styles.module.css";

const Profile = (): JSX.Element => {
	return <div className={styles.container + " flex-center"}></div>;
};

export default Profile;
